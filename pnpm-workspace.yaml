packages:
  - "apps/*"
  - "packages/*"

# create-next-app(pnpm 10)이 apps/web에 넣었던 설정을 루트로 통합
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver

# puppeteer postinstall = Chromium 다운로드 허용(워커 HTML→PDF 렌더용)
onlyBuiltDependencies:
  - puppeteer
